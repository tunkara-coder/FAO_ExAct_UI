git init               # Run this if you haven't already initialized git
git add .
git commit -m "Initial project structure"
git branch -M main     # Set your main branch name to 'main'
git remote add origin <your-repo-url>  # Only if remote is not set
git push -u origin main